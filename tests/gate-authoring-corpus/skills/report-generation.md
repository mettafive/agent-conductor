# Skill: report-generation

**Intent:** Generate a report whose every number traces to the underlying dataset query.

**Grounding source (truth the work is checked against):** the dataset query results

**Shape:** single pass.

## Procedure
1. Run the source queries and snapshot their results into the grounding bundle.
2. Write the report.
