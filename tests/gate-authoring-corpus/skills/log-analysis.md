# Skill: log-analysis

**Intent:** Identify the root-cause incident from logs; every claim cites a real log line.

**Grounding source (truth the work is checked against):** the raw log lines

**Shape:** single pass.

## Procedure
1. Ingest the raw logs into the grounding bundle (every line addressable).
2. Write the root-cause analysis.
