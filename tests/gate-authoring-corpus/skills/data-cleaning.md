# Skill: data-cleaning

**Intent:** Clean a dataset: no nulls in required cols, dedup, valid types; row provenance kept.

**Grounding source (truth the work is checked against):** the raw dataset

**Shape:** single pass.

## Procedure
1. Load the raw dataset + its schema into the grounding bundle.
2. Clean the dataset.
