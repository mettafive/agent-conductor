# Skill: csv-clean

**Shape:** single pass.

**Distinct work-units this skill performs** (each is a phase the user will look for on the board):

- **profile** — profile, inspect dataset.
- **clean** — clean, fix values.
- **normalize dates** — normalize date, date format.  _(visibility divider — no hard gate needed)_
- **trim whitespace** — trim whitespace, strip spaces.  _(visibility divider — no hard gate needed)_
- **export** — export, write csv.

## Procedure
1. profile — profile, inspect dataset.
2. clean — clean, fix values.
3. normalize dates — normalize date, date format.
4. trim whitespace — trim whitespace, strip spaces.
5. export — export, write csv.
