# Skill: gdpr-export

**Shape:** iterates over each subject in subjects.

**Distinct work-units this skill performs** (each is a phase the user will look for on the board):

- **gather data** — gather data, collect records.
- **redact** — redact, mask pii.
- **format package** — format package, machine readable.  _(visibility divider — no hard gate needed)_
- **deliver** — deliver, send export.

## Procedure
For each subject in subjects:
  1. gather data — gather data, collect records.
  2. redact — redact, mask pii.
  3. format package — format package, machine readable.
  4. deliver — deliver, send export.
