# Skill: deploy-flow

**Shape:** single pass.

**Distinct work-units this skill performs** (each is a phase the user will look for on the board):

- **record baseline** — record baseline, current version.
- **deploy** — deploy, rollout.
- **smoke flows** — smoke flow, user journey.  _(visibility divider — no hard gate needed)_
- **health check** — health check, probe endpoint.
- **rollback ready** — rollback, revert path.

## Procedure
1. record baseline — record baseline, current version.
2. deploy — deploy, rollout.
3. smoke flows — smoke flow, user journey.
4. health check — health check, probe endpoint.
5. rollback ready — rollback, revert path.
