# Skill: form-builder

**Shape:** single pass.

**Distinct work-units this skill performs** (each is a phase the user will look for on the board):

- **define fields** — define fields, form schema.
- **add validation** — add validation, rules.
- **conditional logic** — conditional logic, branching.  _(visibility divider — no hard gate needed)_
- **publish form** — publish form, deploy.

## Procedure
1. define fields — define fields, form schema.
2. add validation — add validation, rules.
3. conditional logic — conditional logic, branching.
4. publish form — publish form, deploy.
