# Skill: doc-rewrite

**Shape:** iterates over each page in pages.

**Distinct work-units this skill performs** (each is a phase the user will look for on the board):

- **read current** — read current, load page.
- **rewrite** — rewrite, clarify.
- **add examples** — code example, add example, snippet.  _(visibility divider — no hard gate needed)_
- **add diagrams** — diagram, mermaid, figure.  _(visibility divider — no hard gate needed)_
- **commit** — commit, push.

## Procedure
For each page in pages:
  1. read current — read current, load page.
  2. rewrite — rewrite, clarify.
  3. add examples — code example, add example, snippet.
  4. add diagrams — diagram, mermaid, figure.
  5. commit — commit, push.
