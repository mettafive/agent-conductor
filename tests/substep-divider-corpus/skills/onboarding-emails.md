# Skill: onboarding-emails

**Shape:** iterates over each stage in stages.

**Distinct work-units this skill performs** (each is a phase the user will look for on the board):

- **define stage** — define stage, trigger.
- **write email** — write email, compose.
- **personalize** — personalize, merge tags.  _(visibility divider — no hard gate needed)_
- **queue** — queue, schedule.

## Procedure
For each stage in stages:
  1. define stage — define stage, trigger.
  2. write email — write email, compose.
  3. personalize — personalize, merge tags.
  4. queue — queue, schedule.
