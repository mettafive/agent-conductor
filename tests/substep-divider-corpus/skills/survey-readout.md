# Skill: survey-readout

**Shape:** single pass.

**Distinct work-units this skill performs** (each is a phase the user will look for on the board):

- **load responses** — load responses, raw data.
- **compute stats** — compute stats, percentages.
- **segment** — segment, by cohort.  _(visibility divider — no hard gate needed)_
- **theme freetext** — free text themes, open responses.  _(visibility divider — no hard gate needed)_
- **write summary** — write summary, report.

## Procedure
1. load responses — load responses, raw data.
2. compute stats — compute stats, percentages.
3. segment — segment, by cohort.
4. theme freetext — free text themes, open responses.
5. write summary — write summary, report.
